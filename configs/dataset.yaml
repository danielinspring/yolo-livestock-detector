# YOLO Dataset Configuration
# Combined model for ride and cowtail detection

# Dataset path (will be updated by preprocessing script)
path: data/processed

# Train/val/test splits
train: images/train
val: images/val
test: images/test

# Number of classes
nc: 2

# Class names
names:
  0: ride
  1: cowtail
