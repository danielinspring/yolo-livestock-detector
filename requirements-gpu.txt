# YOLO Combined Model Project Requirements - GPU Version
# For NVIDIA GPU environments (CUDA 11.8 or 12.1)

# Core YOLO and CV
ultralytics>=8.0.0
opencv-python>=4.8.0
numpy>=1.24.0
pandas>=2.0.0
matplotlib>=3.7.0
seaborn>=0.12.0
pillow>=10.0.0
pyyaml>=6.0
tqdm>=4.65.0
scikit-learn>=1.3.0

# Web UI
streamlit>=1.28.0
plotly>=5.17.0

# PyTorch with CUDA support
# IMPORTANT: Install PyTorch separately first based on your CUDA version
# See installation instructions in DEPLOYMENT.md
# For CUDA 11.8:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu118
# For CUDA 12.1:
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu121

# Optional for advanced features
tensorboard>=2.13.0

# ONNX support (optional)
# onnx>=1.14.0
# onnxruntime-gpu>=1.15.0  # GPU-accelerated ONNX runtime
